<?xml version="1.0" encoding="utf-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:OlaMundoApp" x:Class="OlaMundoApp.MainPage">
    <StackLayout>
        <!-- Place new controls here -->
        <Label Text="Hello Andrei!"
               HorizontalOptions="Center"
               VerticalOptions="CenterAndExpand" 
               BackgroundColor="Blue"
               FontSize="Large"
               TextColor="Silver"
               />
        
        <Label Text="Ola Andrei!"
               HorizontalOptions="Center"
               VerticalOptions="CenterAndExpand"
               BackgroundColor="Red"
               FontSize="70"
               TextColor="White"
               />
        
        <Label Text="Hallo Andrei!"
               HorizontalOptions="Center"
               VerticalOptions="CenterAndExpand"
               BackgroundColor="Gray"
               FontSize="50"
               TextColor="Maroon"
               />
    </StackLayout>
</ContentPage>